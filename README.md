# Aster

A native Linux workspace manager, built in Rust.

## Project Structure

Aster is organized as a Cargo workspace with two crates:

- **`aster-core`** — Library crate containing all business logic.
- **`aster-ui`** — Binary crate providing the graphical interface.

## Build & Run

```sh
cargo run -p aster-ui
```
